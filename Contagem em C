#include <stdio.h>

int main(){
    int n;
    printf("Digite um numero");
    scanf("%d", n);

    while(n >= 0){
        printf("%d", n);
        n++;
    }
    printf("Contagem finalizada\n");
    return 0;
}
